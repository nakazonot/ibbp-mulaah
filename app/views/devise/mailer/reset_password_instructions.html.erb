<%
  edit_password_url = @edit_password_uri.present? ?
                           add_param_to_uri(@edit_password_uri, :reset_password_token, @token) :
                           edit_password_url(@resource, reset_password_token: @token)
%>

<%= t('message.reset_password_instructions_html',
    email: @resource.email,
    reset_password_url: edit_password_url
  )
%>
